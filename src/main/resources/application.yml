server:
  port: 8761
spring:
  application:
    name: Eureka-Server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
management:
  endpoints:
    web:
      exposure:
        include: info